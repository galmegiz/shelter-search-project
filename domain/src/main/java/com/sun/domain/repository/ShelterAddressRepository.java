package com.sun.domain.repository;

import com.sun.domain.entity.ShelterAddress;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ShelterAddressRepository extends JpaRepository<ShelterAddress, Long> {
}
